## Domain Layer

In this folder, we maintain the core business domain logic. Using contracts, we define the structure, functions, requests, and responses. It’s important to note that only the interfaces are defined here — no implementations.