## Presentation

In this folder, we include all the code responsible for handling user input — it acts as the application’s entry point. This layer manages incoming requests, delegates the execution to the business logic, and returns the appropriate response to the user.